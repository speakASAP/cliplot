# Kubernetes Manifests

Kubernetes manifests will be added during
`GOAL-04-kubernetes-vault-rag-deployment`.

Do not create production manifests until:

- app source exists;
- Dockerfile exists;
- Vault path and ExternalSecret keys are approved;
- deployment-readiness gate is updated;
- public smoke plan is defined.
