apiVersion: v1
kind: Service
metadata:
  name: cliplot
  namespace: statex-apps
  labels:
    app: cliplot
    managed-by: codex
spec:
  type: ClusterIP
  selector:
    app: cliplot
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      name: http
