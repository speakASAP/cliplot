apiVersion: v1
kind: Service
metadata:
  name: cliplot-service
  namespace: statex-apps
  labels:
    app: cliplot-service
    managed-by: codex
spec:
  type: ClusterIP
  selector:
    app: cliplot-service
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      name: http
